# Keep Scraped Data Script-Owned

Qme will not own large scraping outputs or become the system of record for scraped data. Scripts decide where to persist their results, while Qme stores lifecycle state, progress, logs, small structured metadata, and optional artifact pointers needed for monitoring and orchestration.
