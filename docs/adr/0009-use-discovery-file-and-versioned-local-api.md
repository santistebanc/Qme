# Use a Discovery File and Versioned Local API

Qme Node clients will connect through a versioned localhost API and can discover the running app through defaults, environment overrides, and a Discovery File in a standard app-data location. This keeps local integration convenient while allowing ports, versions, and startup state to change without hardcoding every client.
